version: "3.9"

services:
  app:
    build: .
    container_name: dclm-attendance
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./exports:/app/exports
    restart: unless-stopped
